providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd server && npm install", "cd combined-app && npm install"]

[phases.build]
cmds = ["cd combined-app && npm run build"]

[start]
cmd = "cd server && node server.js"
